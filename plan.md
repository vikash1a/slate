I want to build a app like notion

* **Frontend:** React or Vite + TypeScript (for a snappy, type-safe UI).

* **Editor Engine:** Don't write a rich text editor from scratch. Use **BlockNote** or **Editor.js**. These are block-based editors out of the box, giving you that exact "slash-command (`/`)" Notion feel.

* **Styling:** Tailwind CSS for fast UI building.

* Storage - Google Firebase using goggle login

This would be purely client side app connecting directly to firebase using google login.
